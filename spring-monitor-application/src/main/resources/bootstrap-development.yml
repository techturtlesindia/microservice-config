server.port: 8201

spring.cloud.config:
    discovery.enabled: false
    fail-fast: false

turbine:
  appConfig: spring-app-ui,mail-service,proxy-server
  clusterNameExpression: "'default'"
  instanceUrlSuffix: application/hystrix.stream
